# 执行计划
## 当前迭代：视频预览与真实个人介绍
1. 更新最新契约：12 秒静音循环预览、完整本人介绍与服务单位。已完成规格审查；用户指令已明确，无额外决策依赖。
2. 素材：prepare-media.mjs 生成 5 部 12 秒预览，assets 优化肖像；FFprobe 验证时长、无音轨、原片不变。
3. 首页：预览生命周期与视频纹理接入，保留连续圆柱转场和点击播放；验证时间实际推进、切片与暂停。
4. About：真实文案、肖像、服务单位和滚动动效；验证桌面与移动端布局。
5. 自动化及浏览器回归，更新 validation.md 与 GEB 地图。

当前状态：1–5 已执行，视频预览与个人介绍用户流已验证；原站逐帧一致性仍未认证，边界见 validation.md 最新迭代。

## 前期已完成记录
1. 参考站观察及规格审查：记录真实界面与个人化边界。产物 requirements.md。
2. 素材准备：生成 src/projects.json，优先匹配用户上传 PNG，其余生成封面；保留原文件。用户修改为静态封面后，前端不再加载初版预览。完成条件：索引每项文件存在。
3. 页面与交互：src 模块实现首页、菜单、作品筛选、详情、关于、联系。完成条件：所有可见入口可用。
4. 验证：HTTP／Range／资源检查，浏览器桌面与移动视口用户流，保存实际证据。完成条件：validation.md 明确通过项与未验证项。

状态：1–4 已执行，本地功能可交付。严格像素／逐帧一致性未认证；详见 validation.md 的差异与验证边界。

## 首页动效修复
1. 已完成：重新测量原站 Three.js 画布、圆柱 transform 与外层透视尺寸。
2. 已完成：hero-motion.js / hero-renderer.js / hero.js 分离连续位置、绘制及交互。
3. 已完成：hero.css 替换旧首页规则；菜单错峰入场和文字翻动；页面入场与首次片头解耦。
4. 已完成本轮验证：浏览器实际动画中间帧、鼠标拖拽、滚轮、刷新入场、手机播放回归；verify-motion.mjs 自动化通过。逐帧一致性边界见 validation.md。

2026-09-18 局部封面任务完成：About 页头选定 film-44，AIGC 分类封面保留 film-40 并上移裁切焦点；桌面与 390px 手机截图验证通过，证据见 validation.md。

2026-09-18 后续封面替换完成：AIGC 分类入口改用独立 aigc-category-node37.jpg，取自用户指定图片节点_37_-_副本.png；发布副本同步完成，桌面和手机显示已验证。
