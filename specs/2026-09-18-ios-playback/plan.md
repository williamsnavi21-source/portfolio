# 计划
1. 在预览分支验证固定影片外部 rewrite 的响应头、Range 与内容；不符合契约则先修正方案。
   预览部署 Ready，但匿名协议请求被预览登录保护重定向，不能当作视频响应。先发布未被页面引用的单一兼容路由到生产做真实匿名 Range 验证；现有播放地址不改变，再决定是否接入播放器。
2. 接入共享播放回退状态，覆盖弹层和详情，补充失败／恢复／销毁测试并更新 GEB。
3. 构建与线上预览用户流通过后发布 main；记录真实协议结果与待用户 iPhone 验证的边界。
