# 执行计划
## 当前任务：复用旧 Vercel 项目
状态：原项目预览与生产均由 Git 推送自动部署成功；完整影片、首页预览、AIGC 封面已验收。@ 与 www 已切回 Vercel，权威 DNS 正确，正式 About 页面可访问；正在复验递归 DNS 传播后的本机普通直连。下方“当前优先任务／当前任务”为历史阶段记录，已被此阶段取代。
1. 核实 portfolio 项目、生产部署、Git 链路与域名（已通过浏览器验证）。
2. 保留旧 Git 内容及部署回退，在独立目录准备新版源码和 Vercel 构建配置；验证包含 44 作品与 37 OSS 原片地址，排除原片／凭据上传。
   实施调整：旧仓库已克隆到 .tools/portfolio-origin 用于核对。将旧 main 历史合并到当前工作区的预览分支，保留旧素材目录与所有历史；仅入口采用新版。这样日后仍在当前工作区修改并推送，无需维护两份网站源码。公开 OSS 根地址纳入 vercel.json 构建配置以确保预览和生产一致。
3. 将新版发布到旧项目的预览环境，验证页面、深链、媒体后提升为生产。
4. 按该项目实际配置切换 @ / www，验证本机 HTTPS 与浏览器播放；确认 Git 推送自动部署，并记录回退值。films 测试子域名另行清理或复用，不将失败测试称为正式地址。

## 当前优先任务：自定义域名访问诊断
最新进展：films.navivideo.me 测试别名与 CNAME 已配置，但本机 Netlify TLS 仍重置，新域名证书申请 422。用旧 Vercel IP 216.198.79.1 固定请求 navivideo.me 返回 TLS 成功及 200，标题 Navi Wang | film。当前连接的 Vercel 团队无项目，等待用户选择当前账号新建新版或登录旧账号复用。生产 @ / www 未更改，临时 films 仅用于诊断，未作为上线结果。

1. 读取 Netlify 域名、TLS、跳转配置；分别检查权威 DNS 和递归 DNS。
2. 对比默认域名、自定义域名与固定正确 IP 的 HTTPS 结果；区分 DNS、TLS/传输层、平台路由问题。
3. 仅在浏览器控制恢复且证据支持时配置同站点测试子域名；验证 HTTPS 和页面后再考虑生产切换。不得将子域名存在当作修复完成。
4. 记录可复现证据及未解决限制；大陆托管作为备选，当前不购买资源。

进展：1–2 已完成。域名绑定与平台证书状态正常；同一服务器 IP 下默认域名 200，自定义域名 TLS 握手被重置。3 卡在浏览器控制通道错误，重置后仍无法读取阿里云 DNS；等待用户重新打开浏览器。没有新增别名、改 DNS、修改发布文件或购买资源。

## 当前任务：国内 OSS 网页迁移
最新待办：用户已决定办理 ICP 备案并保留大陆地域，并明确同意备案协议，已进入免费自助个人备案表单（/pcContainer/selfEntity）。域名已填；请用户本人填写地区、证件类型、姓名、号码、住所并保存草稿，再继续校验备案资格/服务码和网站信息。证书和 DNS 切换留到满足备案条件后。

当前状态：任务 1–2 已完成；任务 3 因阿里云查询 navivideo.me 未备案而暂停切换，等待用户决定备案或香港托管。74 个前端文件均返回 200，ETag 与本地 MD5 一致。完整影片不重复上传。已保存默认首页 index.html、404 页 index.html、错误响应 200、不开通子目录首页。域名未绑定、OSS 证书未配置，DNS 仍指向 Netlify。期间欠费已由用户充值解决，19 个剩余文件已补传成功。自动更新仍未接通。

1. 核对既有 Bucket 对象和域名证书，准备与 Netlify 相同的公开网页构建产物，确保不覆盖 films/。
2. 上传 index.html、src/、assets/，配置 SPA 首页与回退；核对资源路径、类型和视频地址。
3. 为 navivideo.me 和 www 绑定 OSS 并配置有效证书，验证备案和 HTTPS，再修改两条 DNS；不购买额外服务。
4. 通过本机 HTTPS 直连、浏览器首页/分类/详情/About 和原片播放验收，记录实际状态；自动更新另接 OSS，不再连接 Netlify 生产分支。

## 迁移前计划与记录
1. 生成可重建网页素材与严格构建入口，保持 localhost 行为。
2. 验证缺少媒体地址构建会失败、产物不含原片或凭据、完整页面回归。
3. 用户最新改选阿里云 OSS；确认 OSS 开通状态、地域和计费后，创建专用桶并分片上传 37 部影片，检查 HTTPS 与 Range，设置真实媒体根地址。
4. 接入 Netlify 项目并部署验收合格的产物，验证平台域名；接入 Git 后验证自动部署。
5. 绑定 navivideo.me，验证 HTTPS 与生产分支自动部署。

状态：1–3 完成。37 部影片已上传，用户确认公开读取；专用桶 wang-films-media 已设公共读、关闭桶级阻止公共访问。37 个对象 HTTPS HEAD 均为 200 且长度与本地一致，抽检 Range 为 206。netlify.toml 已设置真实 MEDIA_BASE_URL，本地构建和检查通过。4 已发布生产站（部署 6aacc877e99a5a8204ea94d4），浏览器验证 1080p 完整影片连续播放。5 已完成 navivideo.me 与 www 的绑定、阿里云两条 DNS 切换、证书签发和强制 HTTPS；浏览器正式首页正常。4–5 的 Git 自动部署部分未完成：私有仓库已创建，本地 main 首次提交 8407afb，等待本机 Git 授权后推送并连接 Netlify；不可宣称自动更新已经生效。

上传暂存：.tools/oss-upload/films/ 使用原片硬链接保留原目录，37 个文件合计 9,430,865,421 字节，不额外拷贝原片。控制台扫描显示 37 文件、8994.0MB，对象前缀为 films/。后续 MEDIA_BASE_URL 必须包含 /films（待真实 HTTPS 接通后配置），不能把存储桶根直接用作媒体根。

Cloudflare R2 路线已被用户新选择取代，不再创建 R2 资源或迁移 DNS。网站仍使用 Netlify；影片直接使用 OSS 自带 HTTPS 地址，不额外创建 media 子域和证书。Agent 承担配置和上传，仅登录验证、必要计费/协议确认由用户完成。
